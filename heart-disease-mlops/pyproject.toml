[tool.pytest.ini_options]
# Guarantees `src` and `app` are importable when CI runs bare `pytest`
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra"

[tool.flake8]
max-line-length = 100
extend-ignore = ["E203", "W503"]
exclude = [".git", "__pycache__", "mlruns", "build", "dist"]
